---
interface Props {
  class?: string;
  accent?: boolean;
  ariaLabel?: string;
}

const { class: className = '', accent = true, ariaLabel }: Props = Astro.props;
---

<section class={`glass-card ${accent ? 'glass-card-accent' : ''} ${className}`} aria-label={ariaLabel}>
  <slot />
</section>


