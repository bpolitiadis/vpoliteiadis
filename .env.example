# 🔧 Environment Variables for vpoliteiadis Portfolio
# Copy this file to .env.local and configure the variables below

# ==========================================
# 📊 LOGGING & OBSERVABILITY
# ==========================================

# Log level for application logging
# Options: trace, debug, info, warn, error, fatal
# Default: info (production), debug (development)
LOG_LEVEL=info

# Additional keys to redact from logs (comma-separated)
# These will be automatically censored in log output
REDACTION_EXTRA_KEYS=custom_api_key,user_session

# ==========================================
# 🚨 ERROR TRACKING (SENTRY)
# ==========================================

# Server-side Sentry DSN for error tracking
# Get this from your Sentry project settings
SENTRY_DSN=https://your-key@o123456.ingest.sentry.io/1234567

# Environment name in Sentry (development, staging, production)
SENTRY_ENV=production

# Performance monitoring sample rate (0.0 to 1.0)
# 0.1 = 10% of transactions will be sampled
SENTRY_TRACES_SAMPLE_RATE=0.1

# Client-side Sentry DSN (must have PUBLIC_ prefix)
# This is exposed to the browser, use a different DSN than server-side
PUBLIC_SENTRY_DSN=https://your-client-key@o123456.ingest.sentry.io/1234568

# ==========================================
# 🌐 DEPLOYMENT (Auto-populated by Vercel)
# ==========================================

# These are automatically set by Vercel, but can be manually set for other platforms

# Environment type: development, preview, production
# VERCEL_ENV=production

# Git commit SHA for release tracking
# VERCEL_GIT_COMMIT_SHA=abc123def456

# Current deployment URL
# VERCEL_DEPLOYMENT_URL=https://your-site.vercel.app

# ==========================================
# 🏗️ CUSTOM DEPLOYMENT (Non-Vercel)
# ==========================================

# Git commit SHA (if not using Vercel)
# COMMIT_SHA=abc123def456

# Deployment timestamp (ISO 8601 format)
# DEPLOYED_AT=2025-01-15T10:30:00.000Z

# ==========================================
# 🔧 DEVELOPMENT OVERRIDES
# ==========================================

# Uncomment for development-specific settings

# Enable verbose logging in development
# LOG_LEVEL=debug

# Enable Sentry in development (optional)
# SENTRY_DSN=https://your-dev-dsn@sentry.io/project
# PUBLIC_SENTRY_DSN=https://your-dev-client-dsn@sentry.io/project

# ==========================================
# 📝 USAGE NOTES
# ==========================================

# 1. Copy this file to .env.local for local development
# 2. Add environment variables in Vercel dashboard for production
# 3. Never commit .env.local or actual secrets to git
# 4. Use PUBLIC_ prefix for client-side variables only
# 5. Restart dev server after changing environment variables

# 📚 For detailed documentation, see:
# docs/ENVIRONMENT_VARIABLES.md
# docs/LOGGING.md
